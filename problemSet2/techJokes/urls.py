from django.urls import path

# from . import views

app_name = 'techJokes'

urlpatterns = [
    # Q1 / Q2 / Q3 — joke list, add form, search, hide/reset personalization
    # path('', views.joke_index, name='index'),

    # Q4 — export endpoints
    # path('export/json/', views.export_json, name='export_json'),
    # path('export/xml/', views.export_xml, name='export_xml'),

    # Q5 — import from remote XML URL
    # path('import/', views.import_jokes, name='import'),
]
